const { deploy } = require("./deploy");
const { serve } = require("./serve");
const { fetchData } = require("./fetch_data");
const { sendMessages } = require("./send_messages");

module.exports = {
  deploy,
  serve,
  fetchData,
  sendMessages,
};
